services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: onestream
      POSTGRES_PASSWORD: onestream
      POSTGRES_DB: onestream
    ports:
      - "5432:5432"
    volumes:
      - onestream-postgres:/var/lib/postgresql/data

volumes:
  onestream-postgres:
